# Environment configuration

This file is generated by `php spark env:docs` from `Config\EnvSchema`.
Run `php spark env:check` before starting the application and `php spark env:diff` after changing the schema.

| Variable | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `CI_ENVIRONMENT` | enum (development, testing, production) | required | — | Active CodeIgniter runtime environment. |
| `APP_URL` | url | required | — | Public application URL, including its scheme. |
| `APP_PORT` | int (1–65535) | optional | 8000 | Host port exposed by the web container. |
| `DB_DRIVER` | enum (MySQLi, SQLite3) | required | — | Database driver used by the application. |
| `DB_DATABASE` | string | required when DB_DRIVER=MySQLi | — | MySQL database name. |
| `DB_USERNAME` | string | required when DB_DRIVER=MySQLi | — | MySQL application account. |
| `DB_PASSWORD` | string | required when DB_DRIVER=MySQLi | — | Password for the MySQL application account. |
| `DB_ROOT_PASSWORD` | string | optional | — | MySQL root password used only by Docker Compose. |
| `DB_PORT` | int (1–65535) | optional | 3307 | Host port exposed by the MySQL container. |
| `WEBSOCKET_PORT` | int (1–65535) | optional | 8080 | Host port exposed by the WebSocket server. |
| `WEBSOCKET_URL` | websocket_url | required | — | Browser-accessible WebSocket URL using ws or wss. |
| `WEBSOCKET_TOKEN_SECRET` | string | required | — | Secret reserved for signing WebSocket authentication material. |
| `BENCHMARK_MODE` | bool | optional | false | Allows destructive benchmark data seeding outside production. |
| `push.vapidPublicKey` | string | optional | — | Public VAPID key exposed to browser push clients. |
| `database.default.hostname` | string | optional | — | Direct override for the default database host. |
| `database.default.database` | string | optional | — | Direct override for the default database name. |
| `database.default.username` | string | optional | — | Direct override for the default database user. |
| `database.default.password` | string | optional | — | Direct override for the default database password. |
| `database.default.DBDriver` | enum (MySQLi, SQLite3) | optional | — | Direct override for the default database driver. |
| `database.default.port` | int (1–65535) | optional | — | Direct override for the default database port. |
| `encryption.key` | string | optional | — | CodeIgniter encryption key. |
| `email.protocol` | enum (mail, sendmail, smtp) | optional | — | Email delivery protocol. |
| `email.SMTPHost` | string | optional | — | SMTP server hostname. |
| `email.SMTPUser` | string | optional | — | SMTP account username. |
| `email.SMTPPass` | string | optional | — | SMTP account password. |
| `email.SMTPPort` | int (1–65535) | optional | — | SMTP server port. |
| `email.SMTPCrypto` | enum (tls, ssl) | optional | — | SMTP transport encryption mode. |
| `session.driver` | string | optional | — | Fully-qualified CodeIgniter session handler class. |
| `session.savePath` | string | optional | — | Session handler storage location. |

Secret values must never use placeholders such as `CHANGE-ME`, `password`, or `your-secret-here`.
