# CodeIgniter Chat API Documentation

This repo now maintains a single, source-of-truth public API reference:

- `docs/public-api.md`

That document covers:

- **HTTP routes** (auth, chat UIs, backend endpoints, CSP report)
- **WebSocket protocol** (actions, message schemas, examples)
- **Frontend entrypoints** (jQuery XML/JSON/HTML + Vue)
- **Reusable PHP APIs** (Helpers, Libraries, Models)

If you were linked here from elsewhere, jump to:

- `docs/public-api.md`